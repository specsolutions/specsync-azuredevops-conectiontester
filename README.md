# SpecSync for Azure DevOps Connection Tester

Usage:
1. Build project: `dotnet build`
2. Invoke tool with a project URL and a personal access token (PAT): `dotnet run https://dev.azure.com/myorg/myproject0 koup7...hqa`
